```
apt install zsh && apt install git apt && apt install fonts-powerline -yyy
```
 # *Установка шрифта для тем (много в каких темах присутствует)*
```
apt install fonts-powerline 
```
**Oh My Zsh!**  -- надстройка над ZSH добавляющая поддержку тем, плагин и алиасов.
*Для установки Oh My Zsh! достаточно выполнить одну команду*
```
sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"
```

`nano .zshrc ` Конфиг ZSH 
Плагины: [Github](https://github.com/ohmyzsh/ohmyzsh/wiki/Plugins)
Темы: [Github](https://github.com/ohmyzsh/ohmyzsh/wiki/Themes)


Centos/RHEL
```
pkg install zsh
```
```
sudo yum update && sudo yum -y install zsh
```